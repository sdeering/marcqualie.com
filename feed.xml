---
layout: none
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ site.url }}/feed.xml</id>
  <title>{{ site.name }}</title>
  <subtitle>{{ site.description }}</subtitle>
  <link rel="self" type="application/atom+xml" href="{{ site.url }}/feed.xml"/>
  <link rel="alternate" type="text/html" href="{{ site.url }}"/>
  <updated>{{ site.time || date: "%Y-%m-%dT%H:%M:%S%z" }}</updated>
{% for post in site.posts %}
  <entry>
    <id>{% post_id post.url %}</id>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.url }}"/>
    <published>{{ post.date | date: "%Y-%m-%d%T%H:%M:%S%z" }}</published>
    <updated>{{ post.date | date: "%Y-%m-%d%T%H:%M:%S%z" }}</updated>
    <summary>{{ post.content | xml_escape }}</summary>
{% for tag in tags %}
    <category term="{{ tag }}"/>
{% endfor %}
    <author>
      <name>Marc Qualie</name>
      <uri>https://marcqualie.com</uri>
      <email>marc@marcqualie.com</email>
    </author>
  </entry>
{% endfor %}
</feed>

